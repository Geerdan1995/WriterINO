# Flask Web 框架
Flask>=2.0.0

# Word 文档处理
python-docx>=0.8.11

# PDF 生成
reportlab>=3.6.0

# Excel 处理
pandas>=1.3.0
openpyxl>=3.0.0

# 拼音转换（座位名牌用）
pypinyin>=0.47.0

# Windows Word 转 PDF（可选，仅 Windows 需要）
pywin32>=300; sys_platform == 'win32'

# Werkzeug（Flask 依赖）
Werkzeug>=2.0.0
