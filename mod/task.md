# Web 功能整合任务清单

> **目标**：将会议名牌和座位名牌功能完全整合到现有 Web 应用中，确保架构统一、样式统一。

---

## 目录

- [Phase 1: 会议名牌生成器整合](#phase-1-会议名牌生成器整合)
- [Phase 2: 座位名牌(苏州)生成器整合](#phase-2-座位名牌苏州生成器整合)
- [Phase 3: 座位名牌(深圳)生成器整合](#phase-3-座位名牌深圳生成器整合)

---

## Phase 1: 会议名牌生成器整合 ✅

### Task 1.1: 分析会议名牌代码结构 ✅

**任务描述**：深入分析外部会议名牌项目的代码结构，理解所有功能模块。

**要做什么**：
- [x] 阅读 `external_projects/meeting_badge/index.html`
- [x] 分析 HTML 结构：输入区、预览区、按钮区
- [x] 分析 CSS：6种模式的样式、打印样式
- [x] 分析 JavaScript：预览生成、打印、PDF保存、拼音转换
- [x] 列出所有功能点：6种模式、拼音转换、打印/PDF、爱心动画等

**注意事项**：
- 特别注意 A4 纸张的毫米级精确定位
- 注意打印媒体查询的样式
- 注意 pinyin-pro 库的使用方式

**验收目标**：
- [x] 输出一份功能点清单文档
- [x] 明确哪些功能必须保留，哪些可以优化

---

### Task 1.2: 创建会议名牌页面 HTML 结构 ✅

**任务描述**：在 `main.js` 的 `renderMeetingBadgePage()` 中创建页面结构。

**参考文档**：`mod/meeting_badge_analysis.md` - 「一、HTML 结构分析」

**要做什么**：
- [x] 修改 `renderMeetingBadgePage()` 函数
- [x] 创建页面头部（返回按钮 + 工具信息卡片）
- [x] 创建输入区域：
  - [x] 姓名输入文本框
  - [x] 排版模式选择下拉框
  - [x] 模式六底部文字输入框（条件显示）
- [x] 创建按钮组：生成预览、打印、保存PDF
- [x] 创建预览区域容器
- [x] 创建工具介绍区域

**注意事项**：
- 完全使用 Tailwind CSS 类
- 遵循现有 `renderDocumentToolPage()` 的结构模式
- 保持与公文工具页面一致的视觉风格
- **必须参考** `mod/meeting_badge_analysis.md` 的「一、HTML 结构分析」

**验收目标**：
- [x] 页面结构完整，无功能也能正常显示
- [x] 样式与现有页面统一
- [x] 响应式布局正常

---

### Task 1.3: 迁移会议名牌 CSS 到 Tailwind ✅

**任务描述**：将外部项目的 CSS 转换为 Tailwind CSS 类或自定义 CSS。

**参考文档**：`mod/meeting_badge_analysis.md` - 「三、CSS 样式分析」

**要做什么**：
- [x] 分析哪些样式可以用 Tailwind 类直接替换
- [x] 分析哪些样式需要自定义 CSS（如毫米级定位、打印样式）
- [x] 在 `base.html` 中添加会议名牌专用的自定义 CSS（使用 `<style>` 标签）
- [x] 迁移 6 种模式的样式
- [x] 迁移打印媒体查询样式
- [x] 迁移动画样式（爱心泡泡）

**注意事项**：
- A4 纸张的 `210mm`、`297mm` 等尺寸必须保留
- 打印样式 `@media print` 必须保留
- 毫米级精确定位用自定义 CSS 更合适
- **必须参考** `mod/meeting_badge_analysis.md` 的「三、CSS 样式分析」

**验收目标**：
- [x] 所有样式迁移完成
- [x] 预览区域可以正常显示 A4 纸张大小
- [x] 无样式冲突

---

### Task 1.4: 迁移会议名牌 JavaScript 逻辑 ✅

**任务描述**：将外部项目的 JS 逻辑重构为 `main.js` 中的模块函数。

**参考文档**：`mod/meeting_badge_analysis.md` - 「四、JavaScript 功能分析」、「五、图片资源清单」、「八、整合到 Web 应用的注意事项」

**要做什么**：
- [x] 创建 `initMeetingBadgePage()` 函数
  - [x] 绑定输入框事件
  - [x] 绑定模式选择事件
  - [x] 绑定按钮点击事件
- [x] 创建 `generateMeetingBadgePreview()` 函数
  - [x] 实现 6 种模式的预览生成
  - [x] 实现姓名解析（顿号分隔）
  - [x] 实现两字/三字/英文名的特殊处理
- [x] 创建 `printMeetingBadge()` 函数
- [x] 创建 `saveMeetingBadgePDF()` 函数
- [x] 创建 `createMeetingBadgeHearts()` 函数（爱心动画）
- [x] 创建 `toPinyin()` 函数（拼音转换）
- [x] 创建 `createMode2Card()` 函数
- [x] 创建 `createMode6Card()` 函数

**注意事项**：
- 所有函数都要添加中文注释
- 避免使用全局变量，使用闭包或模块模式
- 图片路径要改为 Flask 静态路径：`/static/assets/meeting_badge/`
- 确保 pinyin-pro 库正确加载
- **必须参考** `mod/meeting_badge_analysis.md` 的「四、JavaScript 功能分析」、「五、图片资源清单」、「八、整合到 Web 应用的注意事项」

**验收目标**：
- [x] 所有功能逻辑迁移完成
- [x] 可以生成 6 种模式的预览
- [x] 无 JavaScript 错误

---

### Task 1.5: 集成使用统计功能 ✅

**任务描述**：会议名牌使用时更新统计数据。

**要做什么**：
- [x] 在打印或保存 PDF 成功后，调用 API 更新使用次数
- [x] 创建 `incrementMeetingBadgeUsage()` 函数
- [x] 更新页面上的统计显示（总帮助人次、节省时间）

**注意事项**：
- 参考公文工具的统计更新方式
- 不要重复统计（预览不统计，实际打印/保存才统计）

**验收目标**：
- [x] 使用会议名牌后统计数据正确更新
- [x] 页面上的数字实时刷新

---

### Task 1.6: 会议名牌完整测试 ✅

**任务描述**：全面测试会议名牌的所有功能。

**参考文档**：`mod/meeting_badge_analysis.md` - 「六、必须保留的功能」

**要做什么**：
- [x] 测试首页 → 会议名牌页面的导航
- [x] 测试 6 种模式的预览生成：
  - [x] 模式一（单行大字）
  - [x] 模式二（中文+拼音）
  - [x] 模式三（对称旋转）
  - [x] 模式四（带横线标记）
  - [x] 模式五（汇川书院讲师席卡）
  - [x] 模式六（汇川书院学员胸贴）
- [x] 测试两字姓名、三字姓名、英文姓名
- [ ] 测试打印功能（打印预览待完善）
- [ ] 测试保存 PDF 功能（打印预览待完善）
- [x] 测试爱心动画效果
- [x] 测试模式六底部文字输入
- [x] 测试使用统计更新

**注意事项**：
- 特别注意打印效果是否与原项目一致
- 注意 A4 纸张的分页是否正确
- 注意拼音转换是否正确
- **必须参考** `mod/meeting_badge_analysis.md` 的「六、必须保留的功能」

**验收目标**：
- [x] 所有功能正常工作
- [ ] 打印/PDF 效果与原项目一致（打印预览待完善）
- [x] 无 bug

---

## Phase 2: 座位名牌(苏州)生成器整合 ✅

### Task 2.1: 重构苏州座位名牌 generator.py ✅

**任务描述**：将 `code/seat_badge_suzhou/generator.py` 重构为可调用的函数。

**要做什么**：
- [x] 分析现有代码结构
- [x] 将硬编码路径改为参数传入
- [x] 创建 `generate_suzhou_seat_badge()` 函数
  - [x] 参数：excel_path, photo_folder, output_folder, assets_folder
  - [x] 返回：生成的 PDF 路径
- [x] 确保字体文件从 `code/shared/fonts/` 读取
- [x] 确保背景图片从对应的 assets 文件夹读取
- [x] 添加错误处理和日志

**注意事项**：
- 保持原有的 PDF 生成逻辑不变
- 不要破坏原有功能（可以保留原有的 GUI 入口）
- 字体路径要处理好（支持开发环境和打包环境）

**验收目标**：
- [x] 函数可以独立调用
- [x] 生成的 PDF 与原代码一致
- [x] 原有的 GUI 功能不受影响

---

### Task 2.2: 创建苏州座位名牌后端 API ✅

**任务描述**：在 `web/app.py` 中添加苏州座位名牌的 API 端点。

**要做什么**：
- [x] 添加 `POST /api/seat_badge/suzhou/generate` 端点
- [x] 实现文件上传处理：
  - [x] 单个 ZIP 文件（包含 Excel 和照片）
- [x] 实现 ZIP 文件解压
- [x] 自动查找解压后的 Excel 和照片
- [x] 调用 `generate_suzhou_seat_badge()` 函数
- [x] 返回生成的 PDF 文件
- [x] 添加使用统计更新
- [x] 添加临时文件清理

**注意事项**：
- 参考 `/api/document/convert` 的实现模式
- 注意文件上传大小限制（可能需要调大）
- 注意临时文件的清理，避免磁盘占用
- 注意字体文件和背景图片的路径

**验收目标**：
- [x] API 可以正常接收文件
- [x] API 可以生成并返回 PDF
- [x] 使用统计正确更新

---

### Task 2.3: 创建苏州座位名牌前端页面 ✅

**任务描述**：在 `main.js` 中实现苏州座位名牌的前端界面。

**要做什么**：
- [x] 修改 `renderSeatBadgeSuzhouPage()` 函数
- [x] 创建页面结构（与公文工具一致）
- [x] 创建文件上传区域：
  - [x] 单个 ZIP 文件上传（包含 Excel 和照片）
- [x] 创建生成按钮和进度显示
- [x] 创建 PDF 下载按钮
- [x] 创建 `initSeatBadgeSuzhouPage()` 函数
- [x] 实现文件上传逻辑
- [x] 实现 API 调用和进度显示
- [x] 实现 PDF 下载

**注意事项**：
- 完全使用 Tailwind CSS
- 遵循与公文工具相同的交互模式
- 提供清晰的用户指引（需要什么格式的文件）

**验收目标**：
- [x] 页面界面完整美观
- [x] 可以上传文件并生成 PDF
- [x] 可以下载生成的 PDF

---

### Task 2.4: 苏州座位名牌完整测试 ✅

**任务描述**：全面测试苏州座位名牌的所有功能。

**要做什么**：
- [x] 测试首页 → 苏州座位名牌页面的导航
- [x] 测试单个 ZIP 文件上传（包含 Excel 和照片）
- [x] 测试 PDF 生成
- [x] 测试 PDF 下载
- [x] 测试使用统计更新
- [x] 测试错误处理（文件格式不对、缺少文件等）

**注意事项**：
- 准备测试用的 ZIP（包含 Excel 和照片）
- 测试不同的员工数据情况

**验收目标**：
- [x] 所有功能正常工作
- [x] 生成的 PDF 正确
- [x] 无 bug

---

## Phase 3: 座位名牌(深圳)生成器整合 ✅

### Task 3.1: 重构深圳座位名牌 generator.py ✅

**任务描述**：将 `code/seat_badge_shenzhen/generator.py` 重构为可调用的函数。

**要做什么**：
- [x] 分析现有代码结构
- [x] 将硬编码路径改为参数传入
- [x] 创建 `generate_shenzhen_seat_badge()` 函数
  - [x] 参数：excel_path, photo_folder, output_folder, assets_folder
  - [x] 返回：生成的 PDF 路径
- [x] 确保字体文件从 `code/shared/fonts/` 读取
- [x] 确保背景图片从对应的 assets 文件夹读取
- [x] 添加错误处理和日志

**注意事项**：
- 参考 Task 2.1 的实现模式
- 保持原有的 PDF 生成逻辑不变

**验收目标**：
- [x] 函数可以独立调用
- [x] 生成的 PDF 与原代码一致

---

### Task 3.2: 创建深圳座位名牌后端 API ✅

**任务描述**：在 `web/app.py` 中添加深圳座位名牌的 API 端点。

**要做什么**：
- [x] 添加 `POST /api/seat_badge/shenzhen/generate` 端点
- [x] 实现文件上传处理（单个 ZIP 文件，包含 Excel 和照片）
- [x] 调用 `generate_shenzhen_seat_badge()` 函数
- [x] 返回生成的 PDF 文件
- [x] 添加使用统计更新
- [x] 添加临时文件清理

**注意事项**：
- 参考 Task 2.2 的实现模式
- 可以复用苏州版本的文件处理逻辑

**验收目标**：
- [x] API 可以正常接收文件
- [x] API 可以生成并返回 PDF

---

### Task 3.3: 创建深圳座位名牌前端页面 ✅

**任务描述**：在 `main.js` 中实现深圳座位名牌的前端界面。

**要做什么**：
- [x] 修改 `renderSeatBadgeShenzhenPage()` 函数
- [x] 复用苏州版本的前端代码（只需修改文案和 API 端点）
- [x] 创建 `initSeatBadgeShenzhenPage()` 函数

**注意事项**：
- 尽量复用代码，减少重复
- 保持界面风格一致

**验收目标**：
- [x] 页面界面完整美观
- [x] 可以上传文件并生成 PDF

---

### Task 3.4: 深圳座位名牌完整测试 ✅

**任务描述**：全面测试深圳座位名牌的所有功能。

**要做什么**：
- [x] 测试首页 → 深圳座位名牌页面的导航
- [x] 测试 Excel + 照片 ZIP 上传
- [x] 测试 PDF 生成
- [x] 测试 PDF 下载
- [x] 测试使用统计更新

**注意事项**：
- 参考 Task 2.4 的测试内容

**验收目标**：
- [x] 所有功能正常工作
- [x] 生成的 PDF 正确

---

## 总体验收标准

### 完成所有 Phase 后，需要验证：

- [x] 所有工具页面架构统一
- [x] 所有工具页面样式统一（Tailwind CSS）
- [x] 所有工具功能完整可用
- [x] 使用统计功能正常工作
- [x] 无 JavaScript 错误
- [x] 无 Python 错误
- [x] 响应式布局正常
- [ ] 打印/PDF 功能正常（会议名牌打印预览待完善）

---

## 备注

- 每个 Task 完成后，进行简单的测试再进入下一个 Task
- 遇到问题及时记录，必要时调整方案
- 保持代码注释完整，中文注释
- 保持 Git 提交粒度合理，每个 Task 完成后可以提交一次
